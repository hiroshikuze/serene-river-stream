# Coding Conventions

<!-- Populate once the stack is chosen. Examples below are placeholders. -->

## General (enforce now)

- Function length ≤ 50 lines. If longer, split into named helpers.
- Import order: external packages → internal modules → type-only imports.
- No default exports for modules with multiple exports.
- No commented-out code in committed files.

## To be filled in after scaffolding

- Language-specific style (TypeScript strict mode, Python type hints, etc.)
- Formatter / linter commands
- Naming conventions (files, variables, components)
