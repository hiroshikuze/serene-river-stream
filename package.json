{
  "name": "serene-river-stream",
  "version": "1.0.0",
  "description": "A 1-hour ambient 3D river journey synchronized with your wall-clock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
